# Copy this file to .env.local and fill in your values.
# .env.local is git-ignored and should never be committed.

# Google Drive Cloud Sync (optional)
# Create a GCP project, enable the Drive API, create an OAuth 2.0 Web Client,
# and paste the Client ID here. See README → Cloud Sync Setup for full steps.
VITE_GOOGLE_CLIENT_ID=
