import React from 'react';
import { CloudSyncCard } from './CloudSyncCard';

export const CloudSyncSection: React.FC = () => (
  <div className="wp-card settings-section">
    <h2 className="settings-h2">Cloud Sync</h2>
    <CloudSyncCard />
  </div>
);
